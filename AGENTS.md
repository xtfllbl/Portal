## Agent skills

### Issue tracker

Issues and specifications are tracked in this repository's GitHub Issues. See `docs/agents/issue-tracker.md`.

### Domain docs

This repository uses a single-context domain-documentation layout. See `docs/agents/domain.md`.

### UI design

1. **先复用，再调整**：这是同一套 Portal 的可交互原型，服务产品评审和销售演示。先查看用户指定页面及同类页面，复用共享侧栏、顶栏、字体、配色和间距，保持页面互通。仅做 UI 优化时保留业务规则、数据与流程；以用户最新确认的要求为准。
2. **表格保持紧凑**：新增或重做列表默认参考 `2.resellermerchantterminal.html` 的 Terminal List：34px 浅灰表头、11px 字体。已有页面沿用其已确认规格；避免额外上下留白把表头撑高。
3. **操作保持一致**：同类按钮复用项目图标、尺寸与语义色；列表操作参考 `2.agent_list_iso.html` 的 Material Symbols Rounded，不混用不同风格图标。同一操作区按钮等高，表单操作默认 40px、行操作默认 36px。只显示当前状态允许的操作，例如已发布广告不再提供 Save Draft。
4. **下拉可选也可搜**：下拉选择默认同时支持直接点选和输入搜索，并支持键盘操作。代理商／商户／门店等按层级联动；筛选不丢失已有选择。选项列表独立滚动，弹层位置适应视口，避免被弹窗或底部操作栏裁切。
5. **选择前展示限制**：门店、终端等统一提前展示占用状态、数量和对应对象名称，区分当前对象与其他对象。冲突项禁止新增选择，全选跳过不可选项；已有冲突选择保留取消入口，提交时仍校验。已选内容要能集中查看、逐项移除。
6. **界面只留必要内容**：去掉副标题，包括表格里的；不添加用户未要求的字段、步骤、重复按钮和实现细节说明。点击素材即可预览时不再增加 Preview 按钮。演示用切换入口只有用户明确需要时才加入，避免干扰主流程。
7. **错误必须看得见**：保存、发布等操作失败时，在实际滚动容器内自动定位并聚焦错误提示，确保不被固定顶栏或底栏遮挡；说明原因及处理方式，并保留用户输入。可提前识别的问题优先在选择时反馈。
8. **按实际页面验收**：完成后打开修改的具体页面，在桌面和手机尺寸检查表头、按钮高度、下拉搜索、已选状态和错误定位；测试通过不能代替视觉检查。设备预览保持真实屏幕分辨率比例和按键方向，素材保持自身比例；采用用户已确认的产品图。

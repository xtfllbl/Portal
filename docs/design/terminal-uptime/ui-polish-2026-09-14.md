# Service Uptime 动效、文案与密度调整

日期：2026-09-14。状态：用户以“ok”确认 Q26–Q29 及抽屉动效方案；页面、术语和规格已同步，验收见文末。

## 用户已明确的方向

- 从摘要和矩阵打开右侧详情时需要自然的过渡。
- 重新选择 Unreachable time 的表达。
- Data unavailable 和 Not open yet 使用更简洁的无统计结果呈现。
- 收紧矩阵，尤其是 S/N 与 Store 列的横向留白。

## 调整前的已核实状态

- 两个入口共享 `scripts/terminal-uptime-view.js`，但分别直接调用 `showModal()` 和 `close()`；现有抽屉样式没有进入、退出动画。
- `styles/terminal-uptime.css` 的七天桌面规则将 S/N 与 Store 分别设为 22% 和 16%，宽屏时两列随表格一起放大。
- 矩阵行高为 70px，日格为 56px；历史日格也预留了当天进度使用的第三行。
- 无统计结果仍渲染大型状态标题、Online / Unreachable time 数值卡及完整时间轴。截图中的全日采集缺失和营业尚未开始均出现两项 0m。
- 现行术语和 ADR 区分未收到服务通信与已知平台采集故障；本轮仅优化表达，不改日在线率、营业时间、历史权限及分级阈值。

## 已确认的设计树

```text
UI 调整
├─ 抽屉：已要求增加动效
│  └─ 确定进入约 240ms、退出约 180ms，右侧滑入/滑出，遮罩同步淡入/淡出
│     支持系统减少动态效果偏好；两个入口行为一致
├─ Q26 术语：确定 Online / Offline
│  └─ Offline 仅表示未收到预期服务通信，不判断网络或设备故障原因
├─ Q27 无结果呈现：确定日格与详情主指标均用中性的 —
│  ├─ 数据不可评估：详情以小字 No data 说明，已知部分时长仍保留
│  ├─ 尚未营业：详情以小字 Starts at 08:00 等实际营业起点说明
│  └─ 完全无可统计时长不显示两项 0m；全天休息的 Closed 暂保持现状
├─ Q28 无结果交互：确定仍可点击
│  └─ 原因与营业安排保留在详情；接入前/未来日期继续为空且不可点击
└─ Q29 表格密度：确定 S/N、Store 各约 200px，日期列分配剩余宽度
   └─ Store 长名称最多两行；行高约 60px，日格随内容收紧
      当天仍保留进度标识；手机继续使用每台终端一组的七天布局
```

## 文档同步

已将根目录 CONTEXT.md 的规范术语更新为 Offline，并同步 PRD、筛选、图例、时间轴、明细和可访问名称。此轮为可逆的 UI 调整，不新增 ADR；在原有决策中注明显示名称被最新确认替代即可。验收覆盖单终端摘要及矩阵两处的桌面、手机、抽屉进入/退出和无结果状态。


## 验收记录

本地预览：`http://127.0.0.1:8998`。检查页面为 `46.terminal_uptime.html` 和 `1.terminalmanage_nayax.html?sn=WP6267UQ36002376`。本轮未部署。

| 检查 | 结果 |
| --- | --- |
| 桌面矩阵 | 1710px 视口实测 S/N、Store 各 200px；表头 34px；日格 48px，行含边框约 61px；长店名可两行展示。 |
| 抽屉动效 | 两个入口均使用 240ms 入场；Esc 可观测到 up-drawer-out 与遮罩退场动画，完成后关闭并恢复触发日格焦点；关闭按钮同样完成退出。 |
| 日期与后续入口 | 切换 Sep 11 至 Sep 12，入场动画 startTime 不变；Operating Hours 在详情退出后正常打开。摘要 View history 保留 S/N 和所选日期。 |
| 无数据 | WP2013Q321000018 Sep 8 显示 — / No data；无 0m 卡，保留时区、营业安排和原始分段。 |
| 尚未营业 | DEMO-AGT2-001 Sep 14 显示 — / Starts at 08:00；无 0m 卡，仍可查看详情。 |
| 手机 | 390 × 844 检查矩阵七天网格、单终端双列摘要和两处抽屉；按钮为 40px。修复滚动条导致抽屉向左溢出 15px 的问题，左边界归零。分段表保留自身横向滚动。 |
| 筛选 | Show 输入 offline，使用方向键及 Enter 选中 With offline time，显示 8 台匹配样例。 |
| 减少动态效果 | 模拟 prefers-reduced-motion: reduce 后抽屉与遮罩均无动画，Esc 立即关闭；验收后清除模拟。 |
| 计算与无结果回归 | `node --test tests/unit/terminal-uptime.test.cjs`：32/32 通过，包括部分采集缺失仍保留已知 11h 在线与 1h 无数据，不改原始观测。 |
| 构建 | `npm run build` 通过；`git diff --check` 通过。 |


## 矩阵头部与时间条对齐补充

> 最终方案：用户改为在百分比旁放时钟图标，悬浮提示为 `Based on data available at the last update.`。底部 In progress 文字和对应文字图例已移除，表头不改。矩阵、摘要和详情同步使用图标。以下底部文字方案仅记录调整过程，以本段为准。用户同时确认“统一为终端所在时区”，故各终端仍按所在地时区判断当天，跨时区可落在不同日期列。

最终验收：矩阵在 1710px 桌面和 390 × 844 手机视口均已视觉检查，时间条偏移仍为 32px，百分比与时钟均未裁切；图标 title 和无障碍说明与指定英文完全一致，页面不再显示 In progress。单终端摘要在桌面和手机均显示指标旁的时钟，手机点击矩阵日格可正常打开带相同图标的详情。构建、JavaScript 语法和差异空白检查通过。本轮未部署。

2026-09-14 用户根据矩阵截图明确要求：移除 Simulated data 标签与 Refresh 按钮，修复有无 In progress 时进度条不在同一水平线的问题。

- 详细矩阵头部仅保留标题和 Operating Hours，删除刷新按钮的事件绑定及初始化失败分支中的按钮引用。进入页面时仍加载数据，更新时间继续展示。
- 根据用户后续纠正，普通日格保持原有两行居中：16px 指标、4px 时间条及 4px 间距。In progress 独立定位于底部现有留白，使用 10px 行高，不参与居中计算，不为无标签日格预留额外状态行；Closed 时间条同样对齐，无统计结果仍显示 —。
- 此次为可逆的 UI 调整，沿用已有领域术语，不新增 ADR；模拟数据性质和统计规则保持原有定义，现行 PRD 已同步。

验收：在本地 `46.terminal_uptime.html` 的 1710 × 952 与 390 × 844 视口检查，时间条距日格顶部均恢复为原有 32px，指标距顶部 12px；In progress 位于 37–47px，与 32–36px 的时间条不重叠，日格 48px；桌面表头 34px，Operating Hours 按钮 40px，手机无横向撑宽。头部标签与 Refresh 已移除。Show 输入 offline 后方向键与 Enter 可选中 With offline time，显示 8 台匹配终端；手机日格可打开当天详情并保留 In progress。构建、JavaScript 语法检查及 `git diff --check` 通过。本轮未部署。

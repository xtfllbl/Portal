# Portal 右侧抽屉关闭交互

## 已确认要求

2026-09-15，用户确认统一覆盖整个 Portal 的右侧抽屉，包括详情与编辑表单。

- 点击抽屉外的灰色遮罩，收起当前抽屉。
- 点击抽屉内部不触发遮罩关闭。
- 编辑表单有未保存修改时，先提示用户确认是否放弃修改；取消关闭则保留抽屉与输入。
- 关闭按钮、Esc 与遮罩点击使用一致的未保存修改保护。

截图涉及无人值守终端 Service Uptime 与 Uptime Matrix 的 Payment Service 详情；两处均属于本次统一范围。

## 状态

需求已确认，代码与桌面页面验收待完成。

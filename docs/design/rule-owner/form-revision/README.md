# Form revision

Selected: design-1.png; implemented in the existing Customer Alerts page. Supersedes the rejected initial three concepts. Generated using built-in Image Gen, with both user-supplied Assign Transaction screenshots attached. Images design-1.png through design-3.png follow their displayed conversation order. Uses ../mock-accounts.json; the application uses these mock accounts while retaining original seeded IDs.

On selecting Owner Level, render every required field immediately; dependent controls remain visible but disabled until their parent is selected. Search lives inside each dropdown. Preserve the existing account hierarchy, direct-merchant path, owner identity and separate rule-configuration modal. No transaction fields or account creation actions from the reference are in scope.

SP: Service Provider. AGT: Service Provider and Agent. MCH: Service Provider, Agent / Merchant Path, Merchant. STR: all of those plus Store. The frames show the Store branch with its final dropdown open. Empty and disabled states must follow this rule when implemented. Ensure equal action button dimensions and no extraneous list subtitle in final implementation.

## Generation prompt 1

Create realistic production-quality UI mockup, ONE independent design, clear hierarchy and readable typography, purposeful spacing. Target 1296 x 1300 matching attached reference proportions. Both attached images are USER REFERENCES: copy the plain white modal, charcoal typography, thin cool gray borders and understated controls. Adapt the Assign Transaction reference to Select Rule Owner. Title exactly "Select Rule Owner" with close X. NO transaction ID, amount, channel, create merchant/store links, Step headings, subtitles, explanatory paragraphs, trees, permanent list columns, breadcrumbs, wizard progress, decorations, invented functionality, or device/browser frame. Rounded white modal with slim gray scrim, English UI. Cancel and Continue fixed bottom right, identical height and width, Continue black when enabled.
CRITICAL INTERACTION: first choose Owner Level. On choosing a level immediately render ALL fields needed for that level, before selecting any account. Fields NEVER appear one by one. Retain cascading data constraints by disabling children until parent selected; disabled fields stay fully visible. SP level needs SP field only; AGT needs SP and Agent; MCH needs SP, Agent / Merchant Path, Merchant; STR needs all four including Store. Direct merchants is just a navigation path, never an Agent account. Each field is a selection trigger; clicking it opens a bounded floating menu with a separate search input INSIDE the menu and scrolling options. No search-first global picker. Demonstrate Store selected and all four fields visible. Actual fixture 20 SP,20 AGT,50 MCH,70 STR total. Selected SP Universal Processing, path Direct merchants, merchant Harbor Market. Store not selected; show its searchable menu OPEN below the last field with search box "Search stores" and 6 options Downtown, Riverside, Central Station, Airport, West End, Old Town plus scrollbar (13 stores in this branch). Continue disabled gray until Store selection. Menu must fit INSIDE available modal body without covering labels, other fields or footer. Keep all four fields visible above it. Only one design per image.
Direction: Underline form, closest to user reference. Owner Level is a compact closed dropdown showing Store (STR) at top. Below divider, stack four full-width labeled fields vertically, each with label ABOVE and a thin UNDERLINE as the input boundary, right chevron. All four fit in upper 60 percent of modal. Labels Service Provider, Agent / Merchant Path, Merchant, Store; values Universal Processing, Direct merchants, Harbor Market, Select store. Labels 16px and values 17px at design scale; compact balanced 72px field rhythm. Open Store popup full input width, bounded height around 280px, white light shadow, search inset 12px, dense 34px menu rows. No extra sections or huge gaps. Keep screenshot style but substantially more compact than the original reference.

## Generation prompt 2

Create realistic production-quality UI mockup, ONE independent design, clear hierarchy and readable typography, purposeful spacing. Target 1296 x 1300 matching attached reference proportions. Both attached images are USER REFERENCES: copy the plain white modal, charcoal typography, thin cool gray borders and understated controls. Adapt the Assign Transaction reference to Select Rule Owner. Title exactly "Select Rule Owner" with close X. NO transaction ID, amount, channel, create merchant/store links, Step headings, subtitles, explanatory paragraphs, trees, permanent list columns, breadcrumbs, wizard progress, decorations, invented functionality, or device/browser frame. Rounded white modal with slim gray scrim, English UI. Cancel and Continue fixed bottom right, identical height and width, Continue black when enabled.
CRITICAL INTERACTION: first choose Owner Level. On choosing a level immediately render ALL fields needed for that level, before selecting any account. Fields NEVER appear one by one. Retain cascading data constraints by disabling children until parent selected; disabled fields stay fully visible. SP level needs SP field only; AGT needs SP and Agent; MCH needs SP, Agent / Merchant Path, Merchant; STR needs all four including Store. Direct merchants is just a navigation path, never an Agent account. Each field is a selection trigger; clicking it opens a bounded floating menu with a separate search input INSIDE the menu and scrolling options. No search-first global picker. Demonstrate Store selected and all four fields visible. Actual fixture 20 SP,20 AGT,50 MCH,70 STR total. Selected SP Universal Processing, path Direct merchants, merchant Harbor Market. Store not selected; show its searchable menu OPEN below the last field with search box "Search stores" and 6 options Downtown, Riverside, Central Station, Airport, West End, Old Town plus scrollbar (13 stores in this branch). Continue disabled gray until Store selection. Menu must fit INSIDE available modal body without covering labels, other fields or footer. Keep all four fields visible above it. Only one design per image.
Direction: Compact aligned form rows. This design differs by horizontal LABEL/CONTROL alignment. At top Owner Level four compact segmented buttons SP / AGT / MCH / STR with STR charcoal selected. Under that, four persistent horizontal rows with labels LEFT at a consistent 240px width and full-outline rounded select boxes RIGHT, all selects same 48px height. Rows Service Provider = Universal Processing; Agent / Merchant Path = Direct merchants; Merchant = Harbor Market; Store = Select store. All 4 rows visible from outset. No hidden fields. Dividers only where useful; avoid separate cards. Open Store popover anchored to its RIGHT control, matching its width, search input at top then the six mock stores in single list with scroll. A compact composition uses shorter overall modal height around 1000px on a 1296x1300 canvas with dim background; no enormous white void. Labels and controls spaced consistently 24px vertically. Clean restrained Poppins styling, white fields light gray outlines, no blue. This is an ordinary select form like supplied reference, not browser hierarchy.

## Generation prompt 3

Create realistic production-quality UI mockup, ONE independent design, clear hierarchy and readable typography, purposeful spacing. Target 1296 x 1300 matching attached reference proportions. Both attached images are USER REFERENCES: copy the plain white modal, charcoal typography, thin cool gray borders and understated controls. Adapt the Assign Transaction reference to Select Rule Owner. Title exactly "Select Rule Owner" with close X. NO transaction ID, amount, channel, create merchant/store links, Step headings, subtitles, explanatory paragraphs, trees, permanent list columns, breadcrumbs, wizard progress, decorations, invented functionality, or device/browser frame. Rounded white modal with slim gray scrim, English UI. Cancel and Continue fixed bottom right, identical height and width, Continue black when enabled.
CRITICAL INTERACTION: first choose Owner Level. On choosing a level immediately render ALL fields needed for that level, before selecting any account. Fields NEVER appear one by one. Retain cascading data constraints by disabling children until parent selected; disabled fields stay fully visible. SP level needs SP field only; AGT needs SP and Agent; MCH needs SP, Agent / Merchant Path, Merchant; STR needs all four including Store. Direct merchants is just a navigation path, never an Agent account. Each field is a selection trigger; clicking it opens a bounded floating menu with a separate search input INSIDE the menu and scrolling options. No search-first global picker. Demonstrate Store selected and all four fields visible. Actual fixture 20 SP,20 AGT,50 MCH,70 STR total. Selected SP Universal Processing, path Direct merchants, merchant Harbor Market. Store not selected; show its searchable menu OPEN below the last field with search box "Search stores" and 6 options Downtown, Riverside, Central Station, Airport, West End, Old Town plus scrollbar (13 stores in this branch). Continue disabled gray until Store selection. Menu must fit INSIDE available modal body without covering labels, other fields or footer. Keep all four fields visible above it. Only one design per image.
Direction: Two-row compact form grid. Natural square 1296x1300 canvas; modal about 1160x1040 centered. Owner Level full-width segmented radio group with four names SP, AGT, MCH, STR, STR selected black. Below, exactly a TWO BY TWO grid of ordinary outlined select fields (NOT permanent option lists): top row Service Provider left = Universal Processing, Agent / Merchant Path right = Direct merchants; second row Merchant left = Harbor Market, Store right = Select store. Labels ABOVE fields and generous 24px column gutter. ALL four fields visible from the moment STR chosen, no progressive reveals. Input heights 48px. There are no cards around the fields. Store menu OPEN anchored immediately below bottom-right input, same width as input; separate search input inside menu and six rows of Downtown, Riverside, Central Station, Airport, West End, Old Town, scrollbar; menu remains confined inside modal body above footer. Left below Merchant remains clean whitespace, not new summary or sidebar. Form's shorter height shows the entire selection task in one glance. Same charcoal/white reference design language, no blue. Footer buttons identical dimensions, Cancel enabled and Continue muted disabled because no Store picked. No permanent columns, no tree, no icons beside every item, no subtitles.


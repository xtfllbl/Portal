{
  "private": true,
  "scripts": {
    "dev": "vite",
    "test:e2e": "playwright test",
    "test:billing": "node --test tests/unit/billing*.test.*",
    "test:billing:ui": "playwright test --config=playwright.billing.config.cjs",
    "billing:email-samples": "node scripts/render-billing-email-samples.mjs",
    "build": "node scripts/build-static.mjs"
  },
  "devDependencies": {
    "@playwright/test": "1.59.1",
    "vite": "7.1.3"
  },
  "engines": {
    "node": ">=22.13.0"
  },
  "dependencies": {
    "@vercel/blob": "^2.8.0"
  }
}

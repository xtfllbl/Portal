import domain from '../scripts/billing-domain.js';
export const { day, monthlyDate, makeBill, summary, publicView, collect, checkout, retryPayment, stopCollection, renewLink, sendLink, stopped } = domain;
